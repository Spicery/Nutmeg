
DAY03_INPUT_FILE := 'aoc/day03/input'
DAY03_EXAMPLE_FILE := 'aoc/day03/example'

def readInput( file_path ):
    FilePath( file_path ).ReadLines.Select( Split )
end

### Things to implement
###     [x] Comments
###     [x] Character literals
###     [x] Immutable character class
###     [x] Function syntax - fn ARGS =>> BODY endfn
###     [ ] Discard arguments (maybe make holes look like <>)
###     [ ] Zip iterates over vectorclasses
###     [ ] returns in tail-call position

def part1( data ):
    var acc := SeqFrom( fn _ =>> 0 end )
    for d in data:
        acc <- $( _ + if n == `0` then -1 else 1 endif ).Zip( acc, d )
    endif
    return acc
end
